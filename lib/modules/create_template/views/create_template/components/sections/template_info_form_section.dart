import 'package:flutter/material.dart';

class TemplateInfoFormSection extends StatelessWidget {
  const TemplateInfoFormSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
