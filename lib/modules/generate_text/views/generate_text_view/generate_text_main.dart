import 'package:flutter/material.dart';

class GenerateTextMain extends StatelessWidget {
  const GenerateTextMain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mano'),
      ),
      body: Container(),
    );
  }
}
