name: collection_conductor
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.0

# Add regular dependencies here.
isar_version: &isar_version 3.1.0

dependencies:
  # path: ^1.8.0
  equatable:
  isar: *isar_version
  uuid:
  result_dart: ^1.1.0
  dson_adapter: ^1.2.0+2
  path_provider: any 

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  isar_generator: *isar_version
  build_runner: any