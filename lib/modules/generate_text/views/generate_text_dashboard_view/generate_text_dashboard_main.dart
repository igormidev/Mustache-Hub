import 'package:flutter/material.dart';

class GenerateTextDashboardMain extends StatelessWidget {
  const GenerateTextDashboardMain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Text generator'),
      ),
      body: Container(),
    );
  }
}
