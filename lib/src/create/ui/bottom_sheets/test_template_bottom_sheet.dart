import 'package:flutter/material.dart';

class TestTemplateBottomSheet extends StatelessWidget {
  const TestTemplateBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
